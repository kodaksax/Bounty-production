/// <reference types="jest" />

// Ensure Jest globals (describe/it/expect/jest) are available to the
// TypeScript language service in editors that load the project's root
// `tsconfig.json`. The project already depends on `@types/jest`.
