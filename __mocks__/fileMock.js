// Static asset stub for Jest.
//
// Metro turns `require('../assets/images/foo.png')` into an asset reference at
// bundle time; Jest has no such transform and tries to parse the binary as
// JavaScript, which fails the whole suite. Any component test that renders a
// screen with a logo or icon needs this.
module.exports = 1;
